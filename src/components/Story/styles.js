import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container: {
        textAlign: 'center',
    }, name: {
        textAlign: 'center',
        color: '#000',
    }
});

export default styles;