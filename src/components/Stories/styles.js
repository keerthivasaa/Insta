import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    flatList: {
        height: 120,
        flexGrow: 0
      },
    container: {
      marginBottom: 15,
    }
});

export default styles;